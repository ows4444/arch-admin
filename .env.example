# Leave unset in local dev — requests go through the Vite dev proxy (see vite.config.ts)
# since the Arch API doesn't send CORS headers. Set explicitly for production builds,
# pointed at the deployed API origin (which must then CORS-allow this app's origin).
# VITE_API_URL=https://api.example.com
